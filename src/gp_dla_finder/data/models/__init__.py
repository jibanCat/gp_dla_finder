"""Packaged trained-model assets.

Each model is an ``.npz`` of inference arrays plus a ``.json`` of provenance,
produced by ``tools/convert_model.py``. See :mod:`gp_dla_finder.model`.

Trained-model *parameters* are distributed here; the *spectra they were trained
on* are not, and are not necessarily redistributable. See each model's JSON.
"""
